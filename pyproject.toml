[project]
name = "facorty-droid-bot"
version = "0.1.0"
description = "Factory Droid x Discord 智能协同引擎"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "discord.py>=2.3.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
droid-discord = "main:main"
